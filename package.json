{
  "name": "inspector-pr-merge-test",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "echo 'TypeScript project - watching for changes...' && npx nodemon --watch . --ext ts --exec 'echo \"File changed\"'"
  },
  "devDependencies": {
    "typescript": "^5.0.0",
    "nodemon": "^3.0.0"
  }
}